import styled from "styled-components";
import Box from "./Box";

const Title = styled(Box)`
  border-color:#6a91af;
  background: #d8e3f1;
  margin-bottom: 2rem;
  text-align: center;
  font-size: 1.5rem;
  padding: 0.7rem;
`;

export default Title;
